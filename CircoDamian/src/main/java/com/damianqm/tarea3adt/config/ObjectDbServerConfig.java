package com.damianqm.tarea3adt.config;

/**
 * ObjectDB se usa en modo embebido (fichero local), igual que DB4O. No requiere
 * servidor externo ni proceso adicional. La conexión la gestiona directamente
 * IncidenciaRepository con la URL del fichero.
 */
public class ObjectDbServerConfig {
	// Clase vacía — reservada para posible configuración futura del servidor.
}
